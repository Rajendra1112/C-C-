#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
char a[20];
int length;
printf("the the string for its length:\n");
scanf("%s",&a);
length=strlen(a);
printf("the length of given string is %d",length);
}
